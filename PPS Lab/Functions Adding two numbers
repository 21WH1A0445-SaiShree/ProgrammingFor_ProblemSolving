#include <stdio.h>
int add(int, int);
void main()
{
int a, b, c;
printf("Enter a,b\n");
scanf("%d%d", &a,&b);
c = add(a,b);
printf("sum of a,b : %d\n", c);
}
int add (int a, int b)
{
int c;
c = a + b;
return c;
}
